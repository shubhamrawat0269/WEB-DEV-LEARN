# in this lecture , I study about Opening Files in Different Modes

1. We can open files using mainly three different modes i.e.
   { read(r) | write(w) | append(a) | read~write modes = r+, w+, a+ } --> flags

2. There are other modes as well to discover but, we can only explore above as for now.