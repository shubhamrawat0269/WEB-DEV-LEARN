# in this lecture, I study about how to write to a file using fs.write

1. we can use fs.write method to do write operation
2. **fs.write(fd, data_to_write, cb)** 
3. we can close the file using **fs.closeSync(fd)**
