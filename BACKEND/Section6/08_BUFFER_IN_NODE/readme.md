# In this lecture , we are going to study about Buffer in nodejs

1. Nodejs Buffer is a typedArray which is a based on Uint8Array with some extra properties.
2. We cannot access Nodejs Buffer in Browser cuz it's available in Nodejs only

3. Show how we can use node buffer ? 
4. Explore methods like alloc , from & allocUnsafe methods
5. Task For you : 
  A. Create an arrayBuffer namde ab;
  B. Connect your arrayBuffer with Buffer & uInt8Array
  C. Try storing data using uInt8Array
  D. Console node buffer and see the result.

6. You can clearly see the result using debug script and you can clearly state that 
node buffer is uInt8Array i.e. TypedArray with some extra properties and methods.

7. Explore toString method and applied on uInt8Array and nodeBuffer.