const nthTerm = (a,n,d) => a + (n-1) * d;
const sumOfnTerms = (n,a,d) => (n/2) * (2*a + (n-1) * d);

const lastTerm = (a,n,d) => a + (n-1) * d;
const middleTerm = (a,l) => (a+l)/2;

export {nthTerm, lastTerm, middleTerm,sumOfnTerms};