/*
  1. We can use the Boolean function to convert any value to a boolean. This is useful for checking if a value is truthy or falsy.
  2. The following values are falsy in JavaScript:
     - false
     - 0
     - ""
     - null
     - undefined
     - NaN

    3. All other values are truthy, some are:
     - true
     - 1
     - "hello"
     - {}
     - []

    Not Operation in JS - ! (exclamation mark) is used to negate a value. 
    It converts a truthy value to false and a falsy value to true.

The process of converting values to their boolean equivalent (truthy or falsy) in JavaScript is known as "type coercion" or "boolean coercion". This occurs when a value is evaluated in a context that expects a boolean, such as in an if statement or when using the logical NOT operator (!).

*/